# Workspace-specific aliases and functions for example.
# Sourced in both activation paths:
#   direnv  → via the _direnv_ws_extras precmd hook in hooks.zsh
#   manual  → via .zshrc (the ZDOTDIR entrypoint)
#
# Everything here is team-shareable and safe to commit.
# Personal config (accounts, tokens, paths) stays in $HOME files.

# alias k=kubectl
# alias tf=tofu
